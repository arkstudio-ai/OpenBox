"""Generic durable Skill Script job runtime.

docs/SKILL_SCRIPT_RUNTIME_REBUILD_PLAN.md is the owning design document.
The platform core knows jobs, attempts, events, leases and outcomes — never
a specific domain such as video.
"""
