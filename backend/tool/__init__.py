"""Agent tool protocol, functional builtin domains and external adapters.

Public plugin definitions use ``tool.tool``; registration uses ``tool.registry``.
See ``tool.catalog`` for the builtin inventory and the repository's
``docs/reference/TOOLS.md`` for domain ownership and execution boundaries.
"""
