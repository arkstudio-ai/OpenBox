# 原生移动端 1.0.20+31：构建与生产联调记录

## 范围

- 基于已合并的 `main@08f5d952`，服务端为 `https://ai.bossipai.com.cn`，对应生产镜像 `20260916-main-08f5d952`。
- 相比 1.0.19+30 的 `0d64dc1`，包含聊天断线检测、丢失终态恢复、会话状态合并读取、离开聊天页面/切到后台停止轮询、历史分页重试、云桌面接管问题卡和关闭桌面画中画等已有修复。
- 本次把版本更新为 1.0.20+31；将历史回归测试的 fixtures 拆出，使测试文件重新满足 800 行限制；生产网络集成测试的名称和日志改为移动端通用描述；更新 CocoaPods 记录的 Podfile 校验和，插件依赖版本未变。
- 不增加管理端会话轨迹/trace 回放页面。原生超管仍是舰队、技能、订阅、通知、消息五个栏目；保留聊天中已有的思考、工具进度及必要操作卡片。

## 已通过

| 检查 | 结果 |
|---|---|
| `flutter analyze` | 无问题；fixtures 拆分后再次通过 |
| `flutter test` | 381 项全部通过（拆分前完整运行） |
| 历史记录测试拆分后回归 | 24 项全部通过 |
| `scripts/check_locales.sh` | 与 Web locale 逐字节一致 |
| `scripts/check_file_size.sh` | 所有 Dart 文件不超过 800 行 |
| iOS simulator debug 构建 | 通过；明确指定生产 API_BASE / WEB_BASE |
| iPhone 17 Pro / iOS 26.5 | 安装成功，原生落地页、登录页正常，SSO 打开正确的生产认证域名 |

构建产物为 `mobile/build/ios/iphonesimulator/Runner.app`。本次没有上传 TestFlight 或发布 Android 安装包。

## 待完成的生产登录验收

SSO 页面两次返回“账号或密码错误”，第二次已核对账号保持原始小写。
已请求账号持有人确认生产 Logto 凭据，或在模拟器中自行完成登录；没有重置密码或绕过认证。
登录成功前，**不声称真实账号聊天、历史恢复、前后台恢复或管理入口的 iOS 验收通过**。
后续应在这个已安装构建上验证发送短消息、流式回复、返回会话、历史翻页、前后台恢复，
并通过生产 trace 核对入库/投影和业务隔离。凭据不得写入文档、日志或测试代码。

构建完成后，本机 `/usr/bin/git` 的 Xcode 启动包装器开始提示待接受许可证；
使用已安装 Xcode 内的实际 Git 二进制完成仓库操作，没有代替用户接受新协议。
已完成的 iOS 构建和当前运行的模拟器不受影响；后续若需要重新构建，先处理本机工具链状态。
