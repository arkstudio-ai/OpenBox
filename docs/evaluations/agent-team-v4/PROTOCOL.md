# Agent Team evaluation v4 — account credit preregistration

Registered 2026-09-21 before any v4 request. The user removed all independent
team credit caps. v1/v2 observations are retained; v3 was superseded without
execution. This round uses the existing account billing mode and records real
usage, pending/unpriced records and any insufficient-account-credit outcomes.
There is no team budget, speculative model-request headroom or paid-tool limit.
No paid tools are enabled for the text trials.

The coordinator's complete summary is persisted as canonical final prose,
including recovery after a lost tool response. This fixes missing deliverables;
it does not change scoring thresholds or relabel earlier failed observations.

The v2 exact Qwen Flash model, default reasoning, original frozen v1 manifest,
all four groups, 80 text trials, separately blocked 20 real-media trials,
serial case execution, 600-second team limit and 900-second case deadline
remain. Only the initial matching lineup may be confirmed automatically.
Prompts, evidence, quality/cost/time reference thresholds and scoring remain
those in [v2](../agent-team-v2/PROTOCOL.md). Every failed/interrupted observation
is retained. No existing resources are deleted.

Record protocol/source SHA-256 and actual account billing mode before sending
requests. A source/protocol mismatch refuses continuation. Real media remains
blocked until a supported provider, verified tariff and user scope exist.
No v4 execution has occurred at registration.
