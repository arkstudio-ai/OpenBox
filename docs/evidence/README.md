# 原始证据

这里保存截图、日志和 JSON 回执，路径由验收文档引用。目录或文件名中的日期是证据采集时间，
不代表当前运行状态；不要改写原始数据来适配新的结论。

- `a1-*`、`b1/`：桌面通道、工作空间和隔离界面。
- `a5-verify-20260908/`：授权中心的预检、前后端检查与模型验证记录。
- `mobile-auth-center-20260907/`：移动授权中心截图。
- 根目录带日期的 JSON：通知、登录引导、镜像与聊天建议发布回执。

验证范围和解释从[验收目录](../reports/README.md)进入；新增证据使用可识别日期和测试范围，先去除密钥与不必要的个人数据。
