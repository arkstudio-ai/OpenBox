# 两项失败闭环复测（2026-09-23）

结论：本轮 F07（上传边界）和 H04（重复打开历史）均通过。未新增媒体生成，未操作 gw2、修改部署或删除云端文件。

## 环境

前端 openbox-frontend-v2:20260923-gaode-f233fb0，后端 openbox-backend:20260922-gaode-d92cd11，均 healthy；工作区 HEAD 6687bdd。公网控制台与工作区文件逐字节一致，SHA256 57b35324b65920558fdcfc157f68257d2c547de8651d56adad991f85deaad980。

运行中的 nginx 配置 /v1/ 已确认 proxy_read_timeout 900s、proxy_send_timeout 900s。本轮上传客户端 timeout=960 秒。

## F07：通过

- 209,715,200 字节有效合成 WAV，仅上传一次，返回 201，耗时 119.337 秒。
- request_id=req_01M34Z5P9ZEA8C16BZNV0FB2HF；文件 fil_01M34Z66V26QD1XC58J9A69ZHH。
- GET content=302，签名 GET Range=206；Content-Range=bytes 0-43/209715200，44字节WAV头正确。未重复下载整个200MiB。
- 209,715,201 字节反例返回 413 PAYLOAD_TOO_LARGE，耗时43.555秒；request_id=req_01M34Z99TY1PSMS131226R513R。
- 原先上限内文件超过60秒返回504的问题，本次119秒真实请求未复现。上传仍无幂等键，遇到其他超时不应盲目重传；本轮不验证30天自动清理策略。

## H04：通过

使用上轮失败的同一会话 ses_7YBWV3YFXDRX5TYCS7CJ14FAX3。

- 初始2条消息；连续重开两次，均为2条，两个input文件和一张确认卡仍在。
- 切换到旧视频会话显示8条消息，再切回原会话仍显示2条，没有空白或串会话。
- 最新 useSession 清空 messages 时同步清空 dataset.sig，与修复一致。

## 更新后的累计记录

以上轮42项执行表为基线，仅将本次重测 F07/H04 从 FAIL 改为 PASS：累计34通过、0失败、4阻塞/部分完成、4未执行。其余条目的证据与时间仍属于上轮，不能解读为本轮重新跑完42项。

4项阻塞/部分完成：S02实际生成作业档位覆盖，P01/P02/C01本轮新生成及费用对账。4项未执行：A04第二身份/工作区，H05超过200条历史，C02专用低余额，L02专用并发隔离。此前已付费验证的自动恢复/final链路证据保留，本轮未重复花费6积分。

历史测试报告保持原始记录；本文件与更新CSV是两项修复的闭环补充。gw2将来发布需包含相应前端路由和超时配置，本轮未执行该发布。
