# 高德接口手册执行报告

执行时间：2026-09-22 23:32 起（Asia/Shanghai）。后端 d92cd11，控制台/脚本基线 8f3e54a。实际从测试包运行浏览器与脚本，并逐条调用接口；不是复述前轮测试。

## 用例结果

总计 42 项：通过 32，失败 2，阻塞/部分完成 4，未执行 4。逐项记录见《测试执行记录.csv》。本轮不能宣称全流程通过。

## 失败 1：200MiB 文件客户端504、后台实际成功

- F07，合成WAV恰好209715200字节；本次只上传一次。
- 客户端约103.90秒后收到 nginx 504 HTML，无 X-Request-Id，未取得 fil ID。
- 后台只读查询确认同名文件 asset_01M34W69HD1X3VYAJ6WAV2BXAT，size=209715200、status=ready，创建时间15:37:59Z。
- 多1字节（209715201）的独立反例正确返回413 PAYLOAD_TOO_LARGE，request_id=req_01M34W91WPJEDV7ZKFGZKPKYA0。
- 说明：大小校验工作正常，但允许上限内的上传未在公网响应时限内返回；不能直接把504当作未上传而重传。应检查各层超时预算，并提供上传结果确认/幂等机制。本轮未修改任何代理配置。
- 证据：size-boundary.json、upload-status.out。大文件保留供排查，未删除云端资源。

## 失败 2：再次打开同一会话历史为空

- H04：浏览器会话 ses_7YBWV3YFXDRX5TYCS7CJ14FAX3 完成后再次点“打开”，消息区由2条变0条。
- 与此前一致：useSession清空HTML，却未重置新版dataset.sig；相同内容被判定无需重绘。
- 当前临时处理：整页刷新后重新填Key/ID；不要重新发付费需求。

## 已完成的关键实测

- 真实localhost跨域：两张有效同名PNG上传、low建会话、发送、答卡；草稿跨25次轮询保留，提交后重绘也保留。
- API鉴权/错误体、默认及三档位、metadata与素材约束、幂等、重复答卡/拒卡、中止均符合预期。
- 每页2条遍历31页，61个唯一会话ID，无重复且倒序。
- 429限流及等待后恢复通过；已有成片重新签名下载成功，8,173,661字节和SHA256与原文件完全一致。
- 测试包官方脚本preflight实际执行，4/4，退出0。没有额外执行会再次生成的视频完整脚本。

## 尚需完成

- P01/P02/C01：本轮报价为Wan 3.0、1080p、9:16、5秒、6积分，费用确认尚未收到，未提交新媒体生成。此前通过不能算本轮通过。
- S02：三个档位的真实落库映射已核对；实际生成作业参数尚未全部覆盖，因此按完整用例标BLOCKED。
- A04/C02/L02：缺少第二身份、专用低余额账户、专用并发隔离条件，未改变共享账户权限或余额。
- H05：未人为构造超过200条消息；控制台长历史上限依然需单独验收。

## 执行差异与证据

Documents目录中的浏览器文件读取曾停住，未返回上传响应；换成内容完全相同的/tmp副本后立即201/201。记录为本机执行环境问题，未仅凭停住判定接口失败。

所有证据已移除Key、Authorization、Cookie及签名URL。公网产品实现、云端配置、gw2均未修改；无云端删除操作。测试创建的会话与素材保留以便复查。

## 完整10分钟超时结果

独立会话 ses_7YBWV41QHYHZ0D4STPJ3NQ9APP：卡片 expires_at=2026-09-22T15:42:28Z，到点自动 timeout，finish=stop，status=idle；之后答卡409，request_id=req_01M34WGYJ5V8ZZ2WZ8SN4X9QB6。真实浏览器也观察到相同终态。没有提前答卡/拒卡/中止。

## 费用与本轮边界

本轮新建测试会话的累计消耗为 0.467967723967 积分（对话/报价/答卡等）。未提交新媒体生成，6积分视频报价未执行。该数字不包含基础云存储/流量可能的基础设施费用；200MiB验收文件已保留，未删除。
