# 原生移动端 1.0.20+31：构建与生产联调记录

> 文档类型：实施 / 验收记录。版本、测试数量和部署状态只对应文内记录时点。

## 范围

- 基于已合并的 `main@08f5d952`，服务端为 `https://ai.bossipai.com.cn`，对应生产镜像 `20260916-main-08f5d952`。
- 相比 1.0.19+30 的 `0d64dc1`，包含聊天断线检测、丢失终态恢复、会话状态合并读取、离开聊天页面/切到后台停止轮询、历史分页重试、云桌面接管问题卡和关闭桌面画中画等已有修复。
- 本次把版本更新为 1.0.20+31；将历史回归测试的 fixtures 拆出，使测试文件重新满足 800 行限制；生产网络集成测试的名称和日志改为移动端通用描述；更新 CocoaPods 记录的 Podfile 校验和，插件依赖版本未变。
- 不增加管理端会话轨迹/trace 回放页面。原生超管仍是舰队、技能、订阅、通知、消息五个栏目；保留聊天中已有的思考、工具进度及必要操作卡片。

## 已通过

| 检查 | 结果 |
|---|---|
| `flutter analyze` | 无问题；fixtures 拆分后再次通过 |
| `flutter test` | 381 项全部通过（拆分前完整运行） |
| 历史记录测试拆分后回归 | 24 项全部通过 |
| `scripts/check_locales.sh` | 与 Web locale 逐字节一致 |
| `scripts/check_file_size.sh` | 所有 Dart 文件不超过 800 行 |
| iOS simulator debug 构建 | 通过；明确指定生产 API_BASE / WEB_BASE |
| iPhone 17 Pro / iOS 26.5 | 安装、生产 SSO 登录、三轮聊天、后台恢复、冷启动登录恢复、历史分页及管理入口均完成模拟器界面操作验证 |

构建产物为 `mobile/build/ios/iphonesimulator/Runner.app`。本次没有上传 TestFlight 或发布 Android 安装包。

## 已完成的生产 iOS 验收（2026-09-16 07:38–07:46，上海时区）

初次 SSO 失败的原因已经查明：模拟器输入将密码首字母自动变成了大写。
保持用户提供的原始大小写后，生产 Logto 验证成功并自动回到原生应用。
先前要求用户确认凭据的判断过早，现已撤回；没有重置密码、绕过认证或改变认证配置。
凭据没有写入文档、日志或测试代码。

| 原生操作 | 观察结果 |
|---|---|
| 登录后发送短消息 | `iOS Release Check` 会话成功创建，回复 `VERIFIED`，状态收敛为完成 |
| 第二轮生成 20 项文本时切到后台，再返回 | 完整回复恢复显示，包含第 20 项，输入框退出运行状态 |
| 第三轮核对上轮标记 | 正确回复 `IOS-20260916`，上下文保持 |
| 从系统应用切换器关闭 BossIP，再启动 | 先展示原有落地页；点击进入后无需再次输入密码，恢复已登录账号 |
| 冷启动后搜索并打开验证会话 | 三轮消息重新从生产历史加载，最终标记仍在，没有重复用户消息 |
| 打开已有 1,347 轮长会话并向上滚动 | 最新窗口正常加载，向前分页后可见更早的 8 轮记录 |
| 超管舰队和消息页面只读浏览 | 页面数据正常加载；底部仍只有舰队、技能、订阅、通知、消息五栏，没有 trace 回放入口 |

服务器侧同时核对了会话 `session_7YBXC8YWA7EGHWDYXW013XGQ1Z`：

- 业务库为 3 条用户消息、3 条助手消息，会话 `idle`；3 个 `run.finished`，没有工具请求或录制缺口。
- trace 已提交并投影 **125/125** 个事件；worker 无入库失败、blob 上传失败、隔离文件或投影积压；业务库 trace SQL 计数仍为 **0**。
- 所核对会话的 28 次历史请求全部 HTTP 200，包含 1 次 `before` 向前分页和 15 次 `after` 增量读取；nginx 请求耗时最大 **495 ms**。这些是本轮功能验收样本，不能代表高并发容量。

三轮主聊天请求均使用应用当时选中的 Gemini 3.8 Flash，生产 trace 记录如下。标题和建议生成单独统计，不混入主聊天耗时：

| 轮次 | 首个模型输出 | 模型请求总耗时 |
|---|---:|---:|
| 1 | 2,985 ms | 3,043 ms |
| 2 | 3,995 ms | 5,394 ms |
| 3 | 3,553 ms | 3,588 ms |

本次验收结束后，模拟器保留已登录的验证会话。未改变既有定时任务、无影桌面、自动采购或消息发布配置。

构建完成后，本机 `/usr/bin/git` 的 Xcode 启动包装器开始提示待接受许可证；
使用已安装 Xcode 内的实际 Git 二进制完成仓库操作，没有代替用户接受新协议。
已完成的 iOS 构建和当前运行的模拟器不受影响；后续若需要重新构建，先处理本机工具链状态。
