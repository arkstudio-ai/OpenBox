// Path constants live in shared/router so features can build links without
// importing the app layer (boundaries §4.1). Re-exported here for app code.
export { paths, routePatterns } from "@/shared/router/paths"
