# 旧版 Web 客户端

本目录保留早期 Web 实现，供历史问题和迁移对照使用。当前开发入口是
[frontend-v2](../frontend-v2/README.md)，根目录 `make frontend` / `make dev` 也使用 v2。

旧版源码与 [package.json](package.json) 保留原状。本目录不是新增产品功能、UI 修复或部署配置的默认落点。
需要复现旧版本时，根据目标提交的依赖与后端协议建立独立环境，不与当前 v2 共用测试结论。
