<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- The whole app is a client of the backend, so without this a
         release build launches and can do nothing. The Flutter template
         only grants it in the debug and profile manifests, which is why
         running from the IDE never showed the gap. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <application
        android:label="BossIP"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <meta-data android:name="XIAOMI_APPID" android:value="${XIAOMI_APPID}"
            tools:replace="android:value"/>
        <meta-data android:name="XIAOMI_APPKEY" android:value="${XIAOMI_APPKEY}"
            tools:replace="android:value"/>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="com.bossip.bipmobile.PUSH_OPEN"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="ai.bossipai.com.cn" android:pathPrefix="/invite/"/>
            </intent-filter>
        </activity>
        <!-- Where the sign-in browser returns: our task-aware callback hands
             the URI to flutter_web_auth_2 (used by the Logto SDK) and returns
             to the original Flutter task. The SDK validates PKCE/state and
             exchanges the code on the Dart side. The scheme must match
             Env.ssoRedirectUri and the Logto native application. Keep this
             as the only handler for com.bossip.bipmobile://callback: invite
             links use the verified HTTPS route above. A custom-scheme invite
             filter with no host would ignore pathPrefix on Android and also
             match this callback, producing a two-item app chooser. -->
        <activity
            android:name=".AuthCallbackActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity="">
            <intent-filter android:label="flutter_web_auth_2">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="com.bossip.bipmobile" android:host="callback"/>
            </intent-filter>
        </activity>
        <receiver
            android:name=".BossIpJPushReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="cn.jpush.android.intent.RECEIVE_MESSAGE"/>
                <category android:name="${applicationId}"/>
            </intent-filter>
        </receiver>
        <!-- Required by JCore. Keeping the transport in :pushcore makes
             registration and message delivery independent from Flutter UI
             lifecycle while the service itself remains non-exported. -->
        <service
            android:name=".BossIpJCommonService"
            android:enabled="true"
            android:exported="false"
            android:process=":pushcore">
            <intent-filter>
                <action android:name="cn.jiguang.user.service.action"/>
            </intent-filter>
        </service>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <package android:name="com.eg.android.AlipayGphone"/>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
